function matrix(){
  const taille = 10;
  matrix = new Array(taille);
  array = new Array(taille);

  for (let j = 0; j < taille ; j++){
    array[i] = 0;
  }

  for(let i = 0 ; i < taille ; i ++){
    matrix[i] = array;
  }

  return matrix;
}

matrix = matrix();


export default matrix;
